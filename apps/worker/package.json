{
  "name": "@wallet/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc -p tsconfig.json",
    "deploy": "wrangler deploy",
    "test": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.0.2",
    "@wallet/shared": "file:../../packages/shared",
    "hono": "^4.10.6",
    "jose": "^5.10.0",
    "zod": "^3.25.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260428.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.51.0"
  }
}
